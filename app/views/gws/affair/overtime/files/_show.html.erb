<dl class="see">
  <dt><%= @model.t :capital_id %></dt>
  <dd><%= tryb { [ @item.capital.no.presence, @item.capital.name.presence ].compact.join(" ") } %></dd>
</dl>

<%= jquery do %>
  var highlighter = new Gws_Elasticsearch_Highlighter();
  highlighter.render();
<% end %>
